import { Message } from '../services/types';
interface Props {
    message: Message;
}
export default function MessageContainer({ message }: Props): import("react/jsx-runtime").JSX.Element;
export {};
