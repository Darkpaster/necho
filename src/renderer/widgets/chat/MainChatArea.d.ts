import { Chat } from '../../services/types';
interface Props {
    currentChat: Chat;
}
export default function MainChatArea({ currentChat }: Props): import("react/jsx-runtime").JSX.Element;
export {};
